=form_for @gadget, {html: {multipart: true}} do |f|
  - if @gadget.errors.any?
    %div There were problems:
    %ul
      - @gadget.errors.full_messages.each do |message|
        %li= message

  = f.label :name
  = f.text_field :name
  = f.label :image
  = f.file_field :image
  = f.submit

